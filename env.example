## OpsDash Frontend environment variables
# Copy this file to `.env.local` (create it manually) for local development overrides.

# Backend base URL (NO trailing `/api` here; frontend appends `/api` automatically)
VITE_API_BASE_URL=https://opsdash-api.onrender.com

